<mvc:View
	controllerName="konstryx.controller.MasterTemplates"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:f="sap.f"
	xmlns:core="sap.ui.core"
	height="100%">

	<f:DynamicPage id="templatePage" headerExpanded="true" fitContent="false">

		<f:title>
			<f:DynamicPageTitle>
				<f:heading>
					<VariantManagement id="templatesVariant"
					                   supportDefault="true"
					                   supportFavorites="false"
					                   supportPublic="true"
					                   select=".onVariantSelect"
					                   save=".onVariantSave"/>
				</f:heading>
				<f:snappedHeading>
					<Title text="Project Templates" level="H2"/>
				</f:snappedHeading>
				<f:expandedContent>
					<Text text="A construction type with its cost breakdown structure and default resources. Instantiating copies them into a project — later edits to the library never reshape a project already being costed."/>
				</f:expandedContent>
				<f:actions>
					<Button text="Create" type="Emphasized" icon="sap-icon://add" press=".onNotImplemented"/>
				</f:actions>
			</f:DynamicPageTitle>
		</f:title>

		<f:content>
			<Table
				id="templateTable"
				items="{path: 'md>/ProjectTemplates', parameters: {'$orderby': 'code'}, templateShareable: false}"
				growing="true"
				sticky="ColumnHeaders"
				mode="SingleSelectMaster"
				selectionChange=".onSelect"
				class="sapUiResponsiveMargin"
				width="auto">

				<headerToolbar>
					<OverflowToolbar>
						<Title id="tableTitle" text="Templates" level="H4"/>
						<ToolbarSpacer/>
						<Button text="Adapt Filters" icon="sap-icon://filter"
						        tooltip="Filter this list" press=".onAdaptFilters"/>
						<Button icon="sap-icon://excel-attachment" text="Export to Excel"
						        tooltip="Download the list as it is arranged here"
						        press=".onExportExcel"/>
						<Button icon="sap-icon://print" text="Print"
						        tooltip="Print, or save as PDF from the print dialog"
						        press=".onPrint"/>
						<Button icon="sap-icon://action-settings" tooltip="Columns, sort, group and filter"
						        press=".onTableSettings"/>
					</OverflowToolbar>
				</headerToolbar>

				<columns>
					<Column id="code" width="14rem"><Text text="Template"/></Column>
					<Column id="name" minScreenWidth="Tablet" demandPopin="true"><Text text="Name"/></Column>
					<Column id="constructionType" width="12rem"><Text text="Construction type"/></Column>
					<Column id="version" width="7rem"><Text text="Version"/></Column>
					<Column id="scope" width="9rem"><Text text="Scope"/></Column>
					<Column id="owningCompany" width="16rem" minScreenWidth="Desktop" demandPopin="true"><Text text="Owned by"/></Column>
				</columns>

				<items>
					<ColumnListItem>
						<cells>
							<ObjectIdentifier title="{md>code}"/>
							<Text text="{md>name}"/>
							<Text text="{md>constructionType}"/>
							<Text text="{md>version}"/>
							<ObjectStatus
								text="{md>scope}"
								state="{= ${md>scope} === 'GROUP' ? 'Success' : 'Warning' }"
								icon="{= ${md>scope} === 'GROUP' ? 'sap-icon://group' : 'sap-icon://building' }"/>
							<Text text="{= ${md>owningCompany/code} ? ${md>owningCompany/code} + ' · ' + ${md>owningCompany/legalName} : 'Whole group' }"/>
						</cells>
					</ColumnListItem>
				</items>
			</Table>
		</f:content>

		<f:footer>
			<OverflowToolbar>
				<Text id="selectionHint" text="Select a template to instantiate it into a project."/>
				<ToolbarSpacer/>
				<Button id="instantiateButton" text="Instantiate into project" type="Emphasized"
					icon="sap-icon://copy" enabled="false" press=".onInstantiate"/>
			</OverflowToolbar>
		</f:footer>
	</f:DynamicPage>
</mvc:View>
